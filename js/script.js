const track = document.getElementById("track");

    for (let i = 0; i < 20; i++) {
        const p = document.createElement("p");
        p.textContent = "SYNC.IND - NEW IN";
        track.appendChild(p);
    }